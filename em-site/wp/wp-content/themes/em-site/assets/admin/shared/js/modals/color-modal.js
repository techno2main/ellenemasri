(function () {
    'use strict';

    // Wrapper léger: la logique est déplacée dans modals/color-modal/engine.js.
    if (typeof window.emWpAdminColorModal === 'undefined' && typeof window.console !== 'undefined') {
        // No-op: la logique est fournie par le script engine chargé en dépendance.
    }
})();
