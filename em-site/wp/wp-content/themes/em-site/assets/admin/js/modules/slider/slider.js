(function ($) {
    'use strict';

    $(function () {
        if (window.EmSiteSliderAdmin && typeof window.EmSiteSliderAdmin.init === 'function') {
            window.EmSiteSliderAdmin.init();
        }
    });
})(jQuery);
